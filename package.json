{
    "name": "wangazz.github.io",
    "version": "0.0.0",
    "license": "MIT",
    "scripts": {
        "start": "live-server --host=127.0.0.1",
        "start:ci": "live-server --host=127.0.0.1 --quiet",
        "build": "watchify ./js/main.js --outfile=./js/bundle.js --verbose",
        "build:ci": "browserify ./js/main.js --outfile=./js/bundle.js",
        "cypress:open": "cypress open",
        "cypress:run": "cypress run",
        "test": "cypress run"
    },
    "dependencies": {
        "showdown": "^1.9.1"
    },
    "devDependencies": {
        "browserify": "^16.5.0",
        "cypress": "^4.1.0",
        "eslint": "^6.8.0",
        "eslint-plugin-cypress": "^2.10.3",
        "live-server": "^1.2.1",
        "watchify": "^3.11.1"
    }
}
