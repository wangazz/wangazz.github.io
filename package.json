{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "watchify": "watchify ./js/main.js -o ./js/bundle.js -v"
    },
    "dependencies": {
        "showdown": "^1.9.1"
    }
}
